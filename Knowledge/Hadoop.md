# Hadoop 학습 정리

W3M1~W3M6 진행하며 정리한 세팅 절차·개념·명령어·주의사항 레퍼런스.

---

## 0. Docker + Hadoop 클러스터 세팅 절차

### 0.1 사전 준비
- Docker Desktop만 설치. 하둡/자바를 로컬에 직접 설치할 필요 없음(Dockerfile이 이미지 안에서 전부 설치).
- 확인: `docker --version`, `docker compose version`

### 0.2 프로젝트 구조
```
프로젝트폴더/
├── Dockerfile
├── docker-compose.yml
├── entrypoint.sh
└── config/
    ├── core-site.xml
    ├── hdfs-site.xml
    ├── mapred-site.xml
    └── yarn-site.xml
```
- `docker-compose.yml`의 `build: .`은 Dockerfile을 감싸서 호출. Dockerfile을 대체하지 않음.

### 0.3 이미지 빌드
```bash
docker compose build
```
- 최초 빌드는 하둡 tarball 다운로드로 수 분 소요.
- 이미 하둡이 설치된 이전 이미지가 있으면 `FROM 이전이미지:latest`로 재활용해 다운로드 생략 가능.
  단, 제출본은 반드시 자립형(`FROM eclipse-temurin:...`)으로 전환할 것.

### 0.4 컨테이너 실행 및 확인
```bash
docker compose up -d
docker exec -it <컨테이너명> jps
```
- `jps`에 `NameNode`, `DataNode` 등이 안 보이면 `docker logs <컨테이너명>` 확인.

### 0.5 entrypoint.sh 필수 로직
```bash
if [ ! -d "${NAME_DIR}/current" ]; then
    hdfs namenode -format -force -nonInteractive
fi
hdfs --daemon start namenode
...
tail -F ${HADOOP_HOME}/logs/*.log
```
- 포맷 여부를 조건으로 감쌀 것. 무조건 포맷하면 재시작 시 데이터 소실.
- 마지막 `tail -F`로 컨테이너 프로세스를 유지.

### 0.6 HDFS 기본 조작
```bash
docker exec -it <컨테이너명> bash
hdfs dfs -mkdir -p /user/data
hdfs dfs -put -f /파일.txt /user/data/
hdfs dfs -cat /user/data/파일.txt
```
- 업로드할 파일이 컨테이너 안에 없으면 `docker cp`로 호스트에서 복사하거나 컨테이너 안에서 직접 생성.
- `-put` 성공 시 출력 없음(정상).

### 0.7 웹 UI 확인
- http://localhost:9870 → Utilities → Browse the file system
- 루트(`/`)에서 바로 안 보이면 해당 경로(`/user/data` 등)까지 직접 이동.

### 0.8 영속성 확인
```bash
docker compose down      # -v 없이 (볼륨 유지)
docker compose up -d
hdfs dfs -cat /user/data/파일.txt
```
- Named volume + entrypoint의 포맷 스킵 로직이 함께 있어야 유지됨.

### 0.9 멀티 노드 구성 시 변경 사항
- `fs.defaultFS`를 `localhost`에서 컨테이너 hostname(`hdfs://master:9000`)으로 변경.
- 모든 노드를 같은 Docker 네트워크에 연결.
- worker entrypoint에 master 대기 로직 추가:
  ```bash
  until (echo > /dev/tcp/master/9000) 2>/dev/null; do sleep 2; done
  ```
- `config/workers`에 worker hostname 목록 기재.

### 0.10 MapReduce(Hadoop Streaming) 실행
```bash
hadoop jar $HADOOP_HOME/share/hadoop/tools/lib/hadoop-streaming-*.jar -files /mapper.py,/reducer.py -mapper mapper.py -reducer reducer.py -input /입력경로 -output /출력경로
```
- worker 이미지에 python3 설치 필요.
- mapper.py/reducer.py에 셔뱅(`#!/usr/bin/env python3`) + `chmod +x` 필수.
- 재실행 전 출력 폴더 삭제: `hdfs dfs -rm -r /출력경로`

---

## 1. 설정/구현 시 주의사항

| 항목 | 규칙 |
|---|---|
| Docker vs HDFS 파일 이동 | `docker cp`=호스트↔컨테이너, `hdfs dfs -put/-get`=컨테이너 로컬↔HDFS. 둘은 별개 단계이며 자동 연결 안 됨 |
| bind mount 범위 | 설정 폴더는 통째로 마운트하지 말 것. XML 파일 단위로 개별 마운트 (폴더 전체 마운트 시 `hadoop-env.sh` 등 필수 파일 소실) |
| 컨테이너 재시작 방법 | 컨테이너 간 SSH로 서로 재시작시키지 말 것. 호스트에서 `docker restart`/`docker compose restart` 사용. SSH 방식은 sudo 의존성, SIGHUP으로 인한 데몬 종료 등 부작용 발생 |
| Streaming 스크립트 | mapper/reducer 파일에 셔뱅 + 실행권한(`chmod +x`) 필수. 없으면 셸이 파일을 직접 실행하려다 `subprocess failed with code 2` 등 에러 |
| 파일 객체 다루기 | 파일/스트림 객체는 인덱싱·슬라이싱 불가(`f[35:]` 등 에러). 줄 번호 대신 내용 조건으로 필터링. 정 필요하면 `f.readlines()`로 리스트화 후 슬라이싱 |
| 인코딩 | 출처 불분명한 텍스트 파일에서 `UnicodeDecodeError` 발생 시 `encoding="latin-1"` 사용(0~255 모든 바이트 유효, 디코딩 에러 없음). `sys.stdin` 스트리밍 입력도 동일하게 재설정: `io.TextIOWrapper(sys.stdin.buffer, encoding="latin-1", errors="replace")` |
| CSV 파싱 | `split(",")` 대신 `csv.reader(sys.stdin)` 사용(따옴표 안 콤마 처리, 스트리밍 유지). 단, 필드에 콤마가 절대 안 섞이는 단순 CSV(숫자·ID뿐)라면 `split(",")`만으로 충분 |
| MapReduce 재실행 | 출력 폴더가 이미 있으면 `Output directory already exists` 에러. 재실행 전 `hdfs dfs -rm -r` 필수 |
| NameNode 재포맷 | 볼륨이 남아있는 상태에서 재포맷하면 `Incompatible clusterID`. entrypoint의 포맷 스킵 조건을 임의로 제거하지 말 것 |
| 헤더 줄 스킵 | 몇 번째 줄인지가 아니라 내용(`row[0] == "실제헤더문자열"`)으로 판단. 파일이 여러 블록으로 나뉘면 map 태스크마다 데이터 시작 위치가 다르므로, 위치 기반 스킵은 일부 태스크에서 실제 데이터를 누락시킴. 헤더 문자열은 대소문자까지 정확히 일치해야 함(`userID` vs `userId` 같은 오타로 스킵 실패 가능) |

---

## 2. Docker 기초

### 2.1 Dockerfile vs docker-compose
- `build: .` = 현재 폴더 Dockerfile로 빌드.
- `docker compose build` = `docker build -t <이름> .`
- `docker compose up -d` = 빌드(필요 시) + 컨테이너 실행(ports/volumes 적용).

### 2.2 이미지 재사용 (작업용/제출용 토글)
```dockerfile
# -------- [제출용/자립형] : 제출 시 주석 해제 --------
# FROM eclipse-temurin:11-jdk-jammy
# ARG HADOOP_VERSION=3.4.1
# RUN curl ... 하둡 다운로드 ...

# -------- [작업용/FAST] : 이전 이미지 재사용 (제출 시 주석 처리) --------
FROM w3m1:latest
```

### 2.3 재시작 명령
- `docker restart <컨테이너>` / `docker compose restart`: entrypoint 재실행, 데몬 재기동.
- **주의**: `docker-compose.yml`의 `volumes:` 항목을 새로 추가/변경한 경우, `restart`로는
  반영 안 됨. `docker compose up -d --force-recreate`로 컨테이너를 다시 만들어야 함.

### 2.4 bind mount와 Dockerfile COPY의 우선순위
- 컨테이너 **실행 시점**의 bind mount는, 그 경로에 대한 이미지 내 `COPY` 결과를
  **통째로 덮어씀(가림)**. 즉 마운트가 활성화된 동안은 호스트 폴더 내용이 항상 우선.
- 그래서 `COPY scripts/mapper.py /scripts/mapper.py`를 Dockerfile에 항상 넣어둬도,
  `./scripts:/scripts` 마운트가 있으면 개발 중 호스트에서 파일 수정 시 재빌드 없이
  즉시 반영됨 — 토글할 필요 없이 **항상 켜둬도 안전**.
- COPY가 실제로 의미를 갖는 상황: 마운트가 없는 환경(이미지 단독 실행 등)에서
  **안전장치/백업**으로 동작.

### 2.5 어느 노드에 파일이 필요한가 (Hadoop Streaming 기준)
- `-files`로 mapper/reducer를 배포하는 방식이므로, **로컬 파일이 실제로 필요한 곳은
  그 명령을 실행하는 노드(보통 namenode)뿐**. datanode는 YARN 분산 캐시가 자동으로
  파일을 배포해주므로, datanode 쪽 Dockerfile/compose에 mapper.py 등을 마운트·COPY할
  필요 없음.
- 같은 이유로 업로드할 데이터셋도 namenode에만 있으면 됨 (`hdfs dfs -put`이 네트워크로
  datanode에 블록을 전송).

### 2.6 전용 클러스터 vs 기존 클러스터 재사용
- 데이터 규모나 요구사항이 이전 과제와 비슷하면 기존 클러스터 재사용이 자연스러움.
- 과제 명세에 "Docker 파일 제출"이 명시되어 있어도, 이게 곧 "새 이미지를 빌드하라"는
  뜻은 아님 — 대개는 "제출물에 이미지 자체(바이너리)는 빼고 설정 파일만 포함하라"는
  **제출 범위 지시**일 뿐. 문구를 정확히 재확인해서 오판하지 않을 것.
- 다만 과제별로 독립된 새 클러스터(별도 컨테이너명·볼륨)를 구성하는 것도 유효한
  선택지이며, 오히려 "Docker 파일 제출" 요구사항을 더 명확하게 충족시켜줌.

---

## 3. Hadoop 클러스터 아키텍처

### 3.1 데몬 역할
| 데몬 | 역할 |
|---|---|
| NameNode | HDFS 메타데이터 관리 |
| DataNode | 실제 데이터 블록 저장 |
| SecondaryNameNode | NameNode 체크포인트 보조 (백업 아님) |
| ResourceManager | YARN 자원 스케줄러 |
| NodeManager | 노드별 컨테이너 실행 |
| JobHistoryServer | 완료된 작업 이력 조회 |

### 3.2 실행 모드
1. Standalone — 단일 프로세스, HDFS 미사용
2. Pseudo-distributed — 한 노드에서 모든 데몬을 별개 프로세스로
3. Fully-distributed — 여러 노드

### 3.3 W3M 시리즈 클러스터 구성
| 과제 | 노드 구성 | hostname |
|---|---|---|
| W3M1 | 단일 노드 | localhost |
| W3M2a | master + worker1 + worker2 | master/worker1/worker2 |
| W3M2b | namenode + datanode1 + datanode2 | namenode/datanode1/datanode2 |
| W3M3/W3M4/W3M5 | W3M2b 재사용 | 동일 |
| W3M6 | 전용 신규 클러스터로 별도 구성 가능 (이미지명만 `w3m6-*`로 구분) | namenode/datanode1/datanode2 |

- hostname은 설정값과 직접 엮여 있으므로 임의 변경 금지. 이미지/컨테이너 이름에만 과제 접두사 부여.

---

## 4. HDFS

### 4.1 명령어
```bash
hdfs dfs -mkdir -p /path
hdfs dfs -put -f local hdfs경로
hdfs dfs -get hdfs경로 local
hdfs dfs -ls /path
hdfs dfs -cat /path/file
hdfs dfs -rm -r /path
hdfs dfs -stat %r /path/file
hdfs dfsadmin -report
hdfs getconf -confKey <키>
```
- `hadoop getconf`, `yarn getconf`는 존재하지 않는 명령. 모든 설정 조회는 `hdfs getconf`로 통일.

### 4.2 `hdfs getconf`가 YARN/MapReduce 설정까지 조회되는 이유
- 하둡은 4개 XML(core/hdfs/mapred/yarn-site.xml)을 실행 시점에 **하나의 통합 설정
  풀(`Configuration` 객체)로 합쳐서** 메모리에 올림. 파일이 나뉜 건 사용자 편의를
  위한 분류일 뿐, 실행 중에는 출처 구분이 없는 하나의 키-값 저장소가 됨.
- `getconf` 도구는 우연히 `hdfs` 패키지 하위로 개발되었을 뿐, 실제로는 **하둡 전체
  설정 풀을 조회하는 공용 도구**. 그래서 `hdfs getconf -confKey mapreduce.framework.name`
  처럼 YARN/MapReduce 설정도 정상적으로 조회됨.
- 비유: 윈도우 "작업 관리자"가 GPU를 직접 제어하지 않아도 시스템 통합 정보를 보여주는
  것과 같은 구조.

### 4.3 복제 vs 블록 분할
| 구분 | 기준 | 예시 |
|---|---|---|
| 블록 분할 | 파일 크기 > `dfs.blocksize`(기본 128MB) | 227MB 파일 → 블록 2개 |
| 복제 | `dfs.replication` 값만큼 각 블록을 여러 노드에 복사 | 1.2MB 파일 + replication=2 → 블록 1개가 두 노드에 완전 복제 |
- 확인: `hdfs fsck /path/file -files -blocks -locations`
- **복제본 수를 낮추는 이유**: 저장 공간이 정확히 그 배수만큼 절약되고 업로드도
  약간 빨라짐. 단, **MapReduce 연산(map/reduce) 속도 자체엔 영향 없음** — 복제는
  쓰기(write)/저장 단계의 문제이지, 읽기·계산 단계와는 무관.

### 4.4 데이터 지역성
- map 태스크는 해당 블록 보유 노드에 우선 배정. `Data-local map tasks` 카운터로 확인.

### 4.5 "로컬"의 정의
- 하둡 문맥의 "local file system" = 컨테이너 안 리눅스 파일시스템. 호스트(내 PC) 아님.

---

## 5. YARN & MapReduce

### 5.1 태스크 스케줄링
- map 개수 = 입력 스플릿(보통 블록) 개수.
- map/reduce는 독립적으로 스케줄링됨. 노드별 역할 고정 없음.
- 단일 노드는 map/reduce 모두 그 노드에서 실행.
- **태스크 개수가 노드 개수보다 많아도 문제없음**: 한 노드가 자원이 허용하는 한
  여러 컨테이너를 동시에 처리(예: 8GB 노드에서 컨테이너당 512MB면 이론상 16개
  동시 실행 가능). 부족하면 에러가 아니라 대기 후 순차 실행.

### 5.2 리듀서 개수 전략
- 리듀서 개수는 **출력 키(카테고리) 종류의 수**로 결정하는 것이지, 노드 개수와는
  무관.
  - 키가 적음(예: 감성 3분류) → 리듀서 1개가 적절. 여러 개 지정하면 대부분
    빈 출력 파일만 생김.
  - 키가 많음(예: 영화 2만7천 개, 상품 11만 개) → 리듀서 여러 개로 병렬 집계.
- 지정: `-D mapreduce.job.reduces=N`

### 5.3 리듀서 출력 정렬 순서
- Hadoop Streaming은 키를 **문자열(사전순)** 기준으로 정렬. 숫자로 된 키(영화ID,
  상품ID)라도 `1, 10, 100, 11, ...` 순으로 나옴(숫자순 아님).
- 숫자순으로 보고 싶으면 결과를 받은 뒤 별도로 정렬: `sort -t$'\t' -k1,1n`

### 5.4 카운팅 vs 실제 집계값이 필요한 작업
- "값 없이 키만 emit하고 reducer가 줄 수만 센다"는 최적화(단어 수, 카테고리 개수
  등)는 **개수만 필요한 작업에서만** 유효.
- 평균·합계처럼 **실제 숫자를 계산**해야 하는 작업(평점 평균, 리뷰 평균 등)에는
  적용 불가 — reducer가 값을 실제로 파싱해서 누적해야 함.
- 개수와 평균을 **둘 다** 출력해야 하는 작업(예: 상품ID+리뷰개수+평균평점)은
  reducer가 합계(`sum`)와 개수(`count`)를 별도 변수로 누적한 뒤 `count`,
  `sum/count`를 함께 출력.

### 5.5 Shuffle 타이밍
| 단계 | 시작 시점 |
|---|---|
| Shuffle(복사) | 개별 map 태스크 완료 즉시 (다른 map 진행 중이어도 시작) |
| Sort + 최종 Reduce | 전체 map 완료 후 |

### 5.6 분산 캐시(`-files`)
- HDFS 스테이징 업로드 → 해당 작업의 태스크가 배정된 노드에만 다운로드.
- 배포 노드 수는 태스크 수로 결정, `dfs.replication`과 무관.
- 패키지 전체(의존성 포함)를 배포해야 하는 경우(예: pip 패키지)는 `-files`로
  부족함 — 실행 노드의 Docker 이미지에 미리 설치해둬야 함.

### 5.7 명령어
```bash
yarn node -list
yarn node -status <node-id>
yarn application -list -appStates ALL
mapred job -status <job-id>
docker exec -it <container> jps
```
- Web UI 포트: NameNode 9870, ResourceManager 8088, JobHistory 19888, DataNode 9864, NodeManager 8042.
- **모니터링**: 실행 중엔 8088(진행률·태스크 상태·노드별 부하), 완료 후엔
  19888(상세 이력·Counters)에서 확인.

---

## 6. Hadoop Streaming

### 6.1 정의
- 임의의 실행 파일을 mapper/reducer로 사용, stdin/stdout으로 데이터 교환.
- 배치 처리 방식(실시간 아님).

### 6.2 키-값 구분자 (기본 탭, 적용 지점 3곳)
| 지점 | 설정 키 |
|---|---|
| mapper 출력 → 키/값 분리 | `stream.map.output.field.separator` |
| 셔플 결과 → reducer stdin 재구성 | `stream.reduce.input.field.separator` |
| reducer 출력 → 결과 파일 | `mapreduce.textoutputformat.separator` |
- 값 없이 키만 emit해도 reducer에는 `키\t\n` 형태로 전달됨(reducer에서 `.strip()` 처리).

### 6.3 실행 템플릿
```bash
hadoop jar $HADOOP_HOME/share/hadoop/tools/lib/hadoop-streaming-*.jar -files /mapper.py,/reducer.py,/기타파일 -mapper mapper.py -reducer reducer.py -input /입력경로 -output /출력경로
```
- 리듀서 개수 지정: `-D mapreduce.job.reduces=1`

### 6.4 체크리스트
- [ ] 셔뱅(`#!/usr/bin/env python3`)
- [ ] `chmod +x`
- [ ] 재실행 전 출력 폴더 삭제
- [ ] 참조 파일 `-files`에 포함
- [ ] worker 이미지에 python3 설치

---

## 7. Python 구현 참고

### 7.1 stdout
- `print()`의 느림은 flush가 아닌 함수 호출 오버헤드 때문.
- f-string으로 합쳐 `write()` 1회 호출이 여러 번 분할 호출보다 빠름(실측 35~40%).
- stdout/stderr는 별도 통로. 디버그 출력은 `sys.stderr` 사용.

### 7.2 조회 자료구조 (실측, 200만 회 조회 / 8000단어)
| 방식 | 시간 |
|---|---|
| list (선형 탐색) | 91.11초 |
| bisect (이진 탐색) | 0.65초 |
| set (해시) | 0.098초 |
| dict (해시) | 0.090초 |
- 존재 여부 확인은 set/dict 사용. 정렬 기반 이진 탐색도 해시보다 느림.

### 7.3 메모리 (실측, 185MB / 200만 줄)
| 방식 | 메모리 |
|---|---|
| `for row in csv.reader(f):` | 10.3 MB |
| `list(csv.reader(f))` | 914.1 MB |

### 7.4 CSV 파싱
- `csv.reader(sys.stdin)` 사용. `split(",")` 금지(따옴표 안 콤마로 컬럼 어긋남).
- 단, 필드가 전부 숫자/짧은 코드뿐이라 콤마가 절대 안 섞이는 단순 CSV(예: `user_id,parent_asin,rating,timestamp`)라면 `split(",")`만으로 충분하고 더 빠름.

### 7.5 숫자 포맷: 반올림 vs 버림
- `f"{value:.1f}"`, `f"{value:.2f}"` 등은 **반올림**이지 버림(truncate)이 아님.
  `3.86869` → `.1f` → `3.9` (버림이었다면 `3.8`).
- 버림이 필요하면 `math.floor(value * 10) / 10` 등을 별도로 사용해야 함.

---

## 8. 인코딩

| 파일 | 인코딩 |
|---|---|
| Sentiment140 CSV | latin-1 |
| Bing Liu Opinion Lexicon | latin-1 |
| VADER lexicon | 비-UTF8 문자 포함 |
| MovieLens ratings.csv | UTF-8 |
| Amazon Reviews 2023 (0-core rating_only CSV) | UTF-8 (단순 콤마 CSV, `csv` 모듈 없이 `split(",")`로 충분) |

```python
with open(path, encoding="latin-1") as f: ...
sys.stdin = io.TextIOWrapper(sys.stdin.buffer, encoding="latin-1", errors="replace")
```

---

## 9. 감성 분석 사전 자료

| 자료 | 연도 | 특징 | 라이선스 |
|---|---|---|---|
| Bing Liu Opinion Lexicon | 2004 | 긍정~2,000/부정~4,800단어, 이진 분류 | CC BY 4.0 |
| VADER | 2014 | 소셜미디어 특화, 단어당 -4~+4 점수, 이모티콘/은어 포함 | MIT |
| Sentiment140 (데이터셋) | 2009 | 160만 트윗, polarity 컬럼(이모티콘 기반 자동 라벨) | - |

- VADER compound score: `sum / sqrt(sum²+15)`, 분류 임계값 `≥0.05` positive / `≤-0.05` negative / 그 사이 neutral.
- Sentiment140 training set은 polarity=2(neutral) 데이터 없음 → 정답 레이블 재사용 시 neutral 산출 불가.
- VADER 알고리즘 내부 동작은 10장 참고.

---

## 10. VADER 알고리즘 심층 분석 (실제 소스코드 기준)

### 10.1 설치 및 기본 사용
```bash
pip install vaderSentiment
```
```python
from vaderSentiment.vaderSentiment import SentimentIntensityAnalyzer
analyzer = SentimentIntensityAnalyzer()
compound = analyzer.polarity_scores(text)["compound"]
```
- Hadoop Streaming에서 쓸 경우, `-files`로는 패키지 의존성까지 배포 안 되므로
  **mapper가 실행되는 노드(Docker 이미지)에 미리 `pip install vaderSentiment`
  필요**.

### 10.2 처리 흐름 3단계 (`vaderSentiment.py` 소스 기준)

**① 단어별 점수를 문맥으로 보정**
- 사전(lexicon) 기본값(-4~+4)을 가져온 뒤, 바로 주변 단어를 보고 그 자리에서 수정:
  - 부정어(`not`, `never` 등) 존재 → 점수에 음수 배율(`N_SCALAR`)을 곱해 뒤집음
  - 정도부사(`very`, `kind of` 등) → 점수를 증폭/감쇠
  - 그 단어만 대문자이고 문장 내 다른 단어는 아니면 → 점수를 더 키움(`C_INCR`)
  - `but`이 문장에 있으면 → `but` 이전 감성은 0.5배, 이후는 1.5배로 재조정

**② 합산 + 구두점 강조**
```python
sum_s = float(sum(sentiments))
if sum_s > 0: sum_s += 느낌표_개수 * 0.292   # 최대 4개까지 반영
```

**③ 정규화(압축 함수) — 실제 소스코드**
```python
def normalize(score, alpha=15):
    norm_score = score / math.sqrt((score * score) + alpha)
```
- `compound = normalize(sum_s)`. alpha=15는 고정 상수.

### 10.3 "정규화"의 정확한 성격
- **데이터셋 분포 기반 정규화가 아님** (평균/표준편차/최댓값 등 전체 데이터를
  참고하지 않음).
- 문장 하나하나에 **독립적으로 적용되는 고정 수학 공식**으로 -1~1 사이로 압축하는
  것. 성격상 `tanh`/`sigmoid` 같은 활성화 함수(activation function)에 더 가까움.

### 10.4 스코어링 전 텍스트 처리 (하는 것 / 안 하는 것)

| 하는 것 | 안 하는 것 |
|---|---|
| 이모지 → 텍스트 설명 변환 | 전체 소문자 통일(대문자 강조 판단을 위해 원본 케이스 보존, 사전 조회 시에만 그때그때 `.lower()`) |
| 공백 기준 토큰화(단순 `split()`) | 어간 추출(stemming) |
| 단어 앞뒤 구두점만 제거 (단, 제거 후 2글자 이하면 원본 유지 — 이모티콘 보존) | 표제어 추출(lemmatization), 불용어(stopword) 제거 |

### 10.5 분류 임계값
```
compound >=  0.05  → positive
compound <= -0.05  → negative
그 사이            → neutral
```

---

## 11. 데이터셋 규모/정제 관련 개념

### 11.1 k-core 필터링 (0-core vs 5-core)
- 추천시스템 연구에서 흔히 쓰는 데이터 정제 방식: 사용자·상품이 각각 최소 k회
  이상 상호작용한 것만 남기고 나머지 제거.
- **0-core**: 필터링 없음, 원본 그대로. 포괄적이지만 노이즈 많음.
- **5-core**: 사용자·상품 각각 5회 이상만 남김. 노이즈는 적지만 상호작용 적은
  항목이 데이터에서 통째로 빠짐.
- **"정확한 개수 집계"가 목적이면 0-core를 써야 함** — 5-core는 리뷰 적은 상품이
  아예 사라져 집계가 왜곡됨.

### 11.2 ASIN / parent_asin (Amazon 데이터셋)
- ASIN = Amazon Standard Identification Number, 아마존이 쓰는 상품 식별자 용어
  (일반적인 "product_id"에 대응).
- `asin`: 색상·사이즈별 개별 변형 상품 ID. `parent_asin`: 그 변형들을 묶는 상위
  상품 ID. 상품 단위 집계에는 공식 문서가 **parent_asin 사용을 권장**.
- ID 길이는 필요 용량과 무관: ASIN(10자)은 원래 짧게 설계된 구식 카탈로그 번호,
  user_id(28자)는 익명화(해시) 처리 과정에서 길어진 것. 둘 다 실제 필요량(상품
  수천만/사용자 수천만)보다 압도적으로 넉넉한 여유 공간을 가짐.

### 11.3 대용량 데이터 처리 시 진짜 병목
- **RAM이 병목이 아님**: mapper/reducer가 스트리밍 처리(`for line in ...`)라면
  입력 파일이 아무리 커도 메모리 사용량은 거의 일정(실측: 185MB든 그 이상이든
  10MB대 유지).
- **진짜 병목은 다운로드 시간 + HDFS 디스크 공간 + 전체 처리 시간**.
  - 디스크: `dfs.replication` 값만큼 저장 공간이 배수로 필요. 큰 데이터셋을
    다룰 땐 replication을 낮춰(예: 2→1) 공간을 절약할 수 있음(단, 연산 속도엔
    영향 없음 — 4.3 참고).
  - Docker Desktop의 메모리 슬라이더는 macOS 위 리눅스 VM **전체**가 쓰는 총량이지,
    컨테이너별 개별 할당이 아님. 호스트 시스템 전체 사용량은 "활성 상태 보기"
    (Mac) 또는 `docker stats --no-stream`(컨테이너별 실사용)으로 확인.
  - 스왑 메모리는 JVM 성능에 치명적(가비지 컬렉션 급격히 느려짐) — 메모리 부족의
    해법으로 부적절. 여유 메모리가 얼마 없는 상태에서 Docker 메모리 할당량을
    더 늘리는 것도 위험(호스트 OS 자체가 버벅일 수 있음).

---

## 12. 결과 검증 방법론 (재사용 가능한 일반 절차)

MapReduce 작업 결과가 올바른지 확인하는 표준 4단계:

1. **출력 줄 수 = 고유 키 개수 일치 확인**
   ```bash
   hdfs dfs -cat /출력경로/part-* | wc -l
   # 원본에서 고유 키 개수 별도 계산 후 비교
   ```
2. **값 범위 체크** (도메인 지식 기반 sanity check, 예: 평점은 1.0~5.0 사이여야 함)
   ```bash
   hdfs dfs -cat /출력경로/part-* | awk -F'\t' '$N<최소 || $N>최대 {print}'
   ```
   아무 것도 안 나와야 정상.
3. **특정 키 하나를 별도 경로로 직접 검산 후 대조**
   ```bash
   grep ",키값," 원본파일 | awk -F',' '{sum+=$N; n++} END{print n, sum/n}'
   hdfs dfs -cat /출력경로/part-* | grep -P "^키값\t"
   ```
   두 결과가 일치(반올림 오차 이내)하면 로직이 정확하다는 직접적 증거.
4. **Job Counters로 입력 처리량 확인**
   JobHistory UI(19888) → 해당 작업 → Counters → `Map input records`가 원본
   데이터 건수(헤더 제외)와 일치하는지 확인.

---

## 13. 과제 요약

| 과제 | 내용 | 재사용 클러스터 |
|---|---|---|
| W3M1 | 단일 노드, HDFS 기본 조작 | - |
| W3M2a | 멀티 노드, Docker network | W3M1(FAST) |
| W3M2b | 설정 변경+검증, `docker compose restart` | W3M2a + hostname 재구성 |
| W3M3 | Word Count (Streaming) | W3M2b |
| W3M4 | 트위터 감성 분석 (키워드 매칭 / VADER 실제 알고리즘 비교) | W3M2b |
| W3M5 | 영화 평균 평점 (MovieLens 20M) | W3M2b |
| W3M6 | 아마존 상품 리뷰 개수+평균 평점 (Amazon Reviews 2023) | 전용 신규 클러스터 |

---

## 14. 에러 메시지 → 원인 → 해결

| 에러/증상 | 원인 | 해결 |
|---|---|---|
| NameNode가 갑자기 종료됨 (SIGHUP) | SSH/exec 세션 종료 시 백그라운드 프로세스에 신호 전파 | 호스트에서 `docker compose restart` |
| `sudo: command not found` | 다중 노드 스크립트 내부 사용자 전환 로직 | 개별 `--daemon` 명령 직접 호출 |
| 컨테이너가 시작 직후 종료 | 폴더 전체 bind mount로 필수 파일 소실 | 파일 단위 마운트로 변경 |
| `UnicodeDecodeError` | UTF-8이 아닌 파일 | `encoding="latin-1"` |
| `Syntax error`, `subprocess failed with code 2` | 셔뱅/실행권한 누락 | `#!/usr/bin/env python3` + `chmod +x` |
| `Output directory already exists` | 이전 실행 결과 잔존 | `hdfs dfs -rm -r <경로>` |
| `TypeError: object is not subscriptable` | 파일/스트림 객체 인덱싱 시도 | 조건 필터링 또는 `readlines()` 후 슬라이싱 |
| YARN 총 메모리가 기대보다 작음 | `yarn.scheduler.maximum-allocation-mb`가 `yarn.nodemanager.resource.memory-mb`보다 작음 | 두 값을 함께 조정 |
| `No such file or directory` (put 시) | 컨테이너 안에 원본 파일 없음 | `docker cp`로 복사 또는 컨테이너 내 직접 생성 |
| 웹 UI에 업로드한 파일이 안 보임 | 루트 경로만 조회 중 | 실제 파일 경로까지 직접 이동 |
| `ValueError: could not convert string to float` | 헤더 줄이 데이터로 잘못 처리됨(헤더 판별 문자열 오타 등) | 헤더 판별 조건의 문자열이 실제 헤더와 정확히 일치하는지 확인 |
| 출력 결과가 숫자순이 아니라 이상하게 정렬됨 | Hadoop Streaming은 키를 문자열(사전순) 정렬 | 결과를 받은 후 `sort -t$'\t' -k1,1n`으로 재정렬 |
| `docker-compose.yml` 볼륨 변경이 반영 안 됨 | `restart`는 볼륨 재구성을 안 함 | `docker compose up -d --force-recreate` |

---

## 참고 링크

- Hadoop 공식 문서: https://hadoop.apache.org/docs/current/
- HDFS Commands Guide: https://hadoop.apache.org/docs/stable/hadoop-project-dist/hadoop-hdfs/HDFSCommands.html
- Hadoop Streaming: https://hadoop.apache.org/docs/current/hadoop-streaming/HadoopStreaming.html
- Bing Liu Opinion Lexicon: https://www.cs.uic.edu/~liub/FBS/sentiment-analysis.html
- VADER (소스코드): https://github.com/cjhutto/vaderSentiment
- VADER 논문: Hutto & Gilbert, "VADER: A Parsimonious Rule-based Model for Sentiment Analysis of Social Media Text", ICWSM 2014
- Sentiment140: http://help.sentiment140.com
- MovieLens 20M: https://grouplens.org/datasets/movielens/20m/
- Amazon Reviews 2023: https://amazon-reviews-2023.github.io/
- Docker Compose: https://docs.docker.com/compose/